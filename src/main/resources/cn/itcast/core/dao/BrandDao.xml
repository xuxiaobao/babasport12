<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itcast.core.dao.product.BrandDao">
    <resultMap id="baseResultMap" type="java.util.Map">
        <id property="id" column="id" />
        <result property="name" column="name" />
        <result property="description" column="description" />
        <result property="imgUrl" column="img_url" />
        <result property="sort" column="sort" />
        <result property="isDisplay" column="is_display" />
    </resultMap>
    <!--查询品牌 get*-->
    <select id="getBrandListWithPage" parameterType="java.util.Map" resultMap="baseResultMap">
      SELECT id,name,description,img_url,sort,is_display
        FROM bbs_brand
        WHERE is_display = #{isDisplay}
        ORDER BY id DESC LIMIT 0,5
    </select>
    <select id="getBrandCount" parameterType="java.util.Map" resultType="Integer">
      SELECT COUNT(1) FROM bbs_brand WHERE is_display = #{isDisplay}
    </select>
</mapper>
